#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int a, b; cin >> a >> b;
    short need; cin >> need;
    int k; cin >> k;
    
    cout << (need + 20) * 2;
}